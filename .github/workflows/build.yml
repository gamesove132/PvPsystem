name: Build PvPnew

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build with Maven
        run: mvn package -f PvPnew/pom.xml

      - name: Upload JAR
        uses: actions/upload-artifact@v4
        with:
          name: PvPnew
          path: PvPnew/target/PvPnew-1.0.0.jar
